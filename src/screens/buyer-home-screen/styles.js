import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    //   backgroundColor: "#fff",
  },
  firstContainer: {
    marginTop: 5,
    marginBottom: 10,
  },
});

export default styles;
